spring:
  r2dbc:
    url: r2dbc:postgresql://${DB_HOST:localhost:5432}/hexarchdatabase
    username: postgres
    password: "${DB_PASSWORD:12134}"

  data:
    redis:
      host: ${REDIS_HOST:localhost}
      port: ${REDIS_PORT:6379}

  sql:
    init:
      mode: always
adapter:
  jwt:
    secret: "${JWT_SECRET:MiSuperSecretoUltraLargoDe32Caracteres!}"